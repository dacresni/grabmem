this is a program to suck up memory. Originally ment for testing the qouta ability of SGE. 
usage: grabmem interval step [max] [timeout] 
This program allocates memory until the machine runs out or you quit it with ctrl-c